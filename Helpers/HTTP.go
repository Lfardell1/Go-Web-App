package Helpers

// HTTP is a helper function to handle HTTP requests
// We block certain HTTP methods to prevent unwanted requests
// We also set the Content-Type header to application/json

func HTTP() {

}
